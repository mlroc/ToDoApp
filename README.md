# This is a ToDoList App utilizing Xcode, Swift, SwiftUI, and Firebase. 

Firebase was used for user authentication (logging in, signing out) and saving models that the user creates as their "to-dos."
